# Konfigurationsdatei für die Start-Routine.
# Werte können per Umgebungsvariablen überschrieben werden.
PYTHON_BIN="python3"
VENV_DIR="${PROJECT_ROOT}/.venv"
REQUIREMENTS_FILE="${PROJECT_ROOT}/requirements.txt"
HOST="0.0.0.0"
PORT="8000"
SERVER_COMMAND="python -m http.server"
LOG_FILE="${LOG_DIR}/start.log"
DEBUG=0
