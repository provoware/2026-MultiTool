name: PROVOWARE Cloud-Pipeline

on:
  push:
  pull_request:
  workflow_dispatch:
    inputs:
      stufe:
        description: 'Hoechste auszufuehrende Pruefstufe'
        required: true
        default: 'TIEF'
        type: choice
        options:
          - SCHNELL
          - TIEF
          - FREIGABE

permissions:
  contents: read

concurrency:
  group: provoware-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CI: 'true'

jobs:
  schnell:
    name: SCHNELL - Vertrag und Kernpruefung
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Exakten Kandidaten laden
        uses: actions/checkout@v7

      - name: Node.js bereitstellen
        uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: 'npm'

      - name: Gesperrte Abhaengigkeiten installieren
        run: npm ci --no-audit --no-fund

      - name: Abhaengigkeiten auf bekannte Risiken pruefen
        run: npm audit --package-lock-only --audit-level=moderate

      - name: SCHNELL-Pruefung ausfuehren
        run: npm run ci:schnell

      - name: Tauri-Quellstand erkennen
        id: tauri
        shell: bash
        run: |
          if [[ -f src-tauri/Cargo.toml ]]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
            echo "Tauri-Quellstand vorhanden. Rust-Pruefung wird ausgefuehrt."
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
            echo "Tauri-Quellstand noch nicht vorhanden. Rust-Pruefung ist fuer diese Foundation nicht anwendbar."
          fi

      - name: Rust-Werkzeugkette pruefen
        if: steps.tauri.outputs.present == 'true'
        run: |
          rustc --version
          cargo --version
          rustup component add rustfmt clippy

      - name: Rust formatieren, kompilieren und linten
        if: steps.tauri.outputs.present == 'true'
        working-directory: src-tauri
        run: |
          cargo fmt --all -- --check
          cargo check --locked
          cargo clippy --locked --all-targets --all-features -- -D warnings

      - name: Diagnose nur bei Fehler sichern
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: schnell-fehlernachweis
          path: runtime/ci-evidence
          if-no-files-found: ignore
          retention-days: 5

  tief:
    name: TIEF - Integration und Bedienung
    needs: schnell
    if: >-
      github.event_name != 'workflow_dispatch' ||
      inputs.stufe == 'TIEF' ||
      inputs.stufe == 'FREIGABE'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Exakten Kandidaten laden
        uses: actions/checkout@v7

      - name: Node.js bereitstellen
        uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: 'npm'

      - name: Gesperrte Abhaengigkeiten installieren
        run: npm ci --no-audit --no-fund

      - name: Vollstaendige Foundation- und Browserpruefung
        run: npm run ci:tief

      - name: Tauri-Quellstand erkennen
        id: tauri
        shell: bash
        run: |
          if [[ -f src-tauri/Cargo.toml ]]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Linux-Abhaengigkeiten fuer Tauri bereitstellen
        if: steps.tauri.outputs.present == 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf

      - name: Rust-Integrationstests ausfuehren
        if: steps.tauri.outputs.present == 'true'
        working-directory: src-tauri
        run: cargo test --locked --all-targets

      - name: Native Tauri-E2E-Pflicht pruefen
        if: steps.tauri.outputs.present == 'true'
        shell: bash
        run: |
          if [[ ! -f tests/tauri-e2e/package.json ]]; then
            echo "Tauri ist vorhanden, aber tests/tauri-e2e/package.json fehlt."
            echo "Die TIEF-Stufe blockiert, bis echte native E2E-Tests vorhanden sind."
            exit 1
          fi
          cd tests/tauri-e2e
          npm ci --no-audit --no-fund
          npm test

      - name: Fehlernachweise nur bei Fehler sichern
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: tief-fehlernachweis
          path: |
            runtime/ci-evidence
            runtime/browser-acceptance
          if-no-files-found: ignore
          retention-days: 7

  freigabe-vorpruefung:
    name: FREIGABE - Kandidat darf gebaut werden
    needs: tief
    if: github.event_name == 'workflow_dispatch' && inputs.stufe == 'FREIGABE'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Exakten Kandidaten laden
        uses: actions/checkout@v7

      - name: Node.js bereitstellen
        uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: 'npm'

      - name: Gesperrte Abhaengigkeiten installieren
        run: npm ci --no-audit --no-fund

      - name: Freigabefaehigkeit pruefen
        run: node scripts/ci/release-preflight.mjs

      - name: Freigabe-Blockade dokumentieren
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: freigabe-blockiert
          path: runtime/ci-evidence
          if-no-files-found: ignore
          retention-days: 7
